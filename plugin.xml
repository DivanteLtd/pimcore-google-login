<?xml version="1.0"?>
<zend-config xmlns:zf="http://framework.zend.com/xml/zend-config-xml/1.0/">
		<plugin>
            <!-- the plugin name [A-Za-z_] -->
            <pluginName>GoogleLogin</pluginName>

            <!-- a short description -->
			<pluginDescription><![CDATA[Allows using google credentials for logging into Pimcore's admin panel]]></pluginDescription>

            <!-- meta data -->
            <pluginVersion>1.0</pluginVersion>
            <pluginRevision>1</pluginRevision>
            <pluginBuildTimestamp>0</pluginBuildTimestamp>

            <!-- put in the URL for the plugin configuration eg. /plugin/GoogleLogin/admin/index -->
            <pluginIframeSrc>/plugin/GoogleLogin/admin/index</pluginIframeSrc>

            <!-- className of the plugin which extends Pimcore_API_Plugin_Abstract-->
			<pluginClassName>\GoogleLogin\Plugin</pluginClassName>

            <!-- include paths relative to plugin-directory -->
			<pluginIncludePaths>
				<path>/GoogleLogin/lib</path>
			</pluginIncludePaths>

			<!-- namespaces to register with autoloader-->
			<pluginNamespaces>
				<namespace>GoogleLogin</namespace>
			</pluginNamespaces>
		</plugin>
</zend-config>
